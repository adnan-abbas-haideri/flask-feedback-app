# `stringData` is convenient for local practice. For a real cluster, use an
# external secret manager or create this Secret outside source control.
apiVersion: v1
kind: Secret
metadata:
  name: mysql-credentials
  namespace: flask-feedback
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: root
  MYSQL_USER: root
  MYSQL_PASSWORD: root
